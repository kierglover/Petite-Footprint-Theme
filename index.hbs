<header>

    <h1>{{ @blog.title }}</h1>
    <h2>{{ @blog.description }}</h2>

</header>

<main>

<!-- For each one of the posts, display title -->
    {{#foreach posts}}
        <h1><a href="{{url}}">{{title}}</a></h1>
    {{/foreach}}

</main>

<!-- Pagination block -->
{{pagination}}